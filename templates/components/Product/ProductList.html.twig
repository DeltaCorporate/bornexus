<div class="flex flex-wrap gap-5" {{ attributes }}>

    {% for product in products %}

        <div class="flex flex-col p-6 rounded-3xl bg-neutral md:w-1/4 w-80 h-96">
            <img src="https://via.placeholder.com/200x150" alt="{{ product.name }}" class="card-img-top">
            <div class="card-body">
                <div class="flex justify-between items-center">
                    <h5 class="card-title">{{ product.name }}</h5>
                    <span class="text-darkgrey text-lg">{{ product.price }} €</span>
                </div>
                <p class="card-text">{{ product.description }}</p>
            </div>
        </div>

    {% endfor %}

</div>
